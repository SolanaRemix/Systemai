/** Log viewer placeholder panel. */
export const LogViewerPanel = (): JSX.Element => (
  <section className="rounded bg-slate-900 p-4 text-sm text-slate-200">Execution logs are persisted to SQLite and surfaced here.</section>
);
