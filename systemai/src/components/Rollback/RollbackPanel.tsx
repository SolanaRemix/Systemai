/** Rollback placeholder panel with snapshot safety model. */
export const RollbackPanel = (): JSX.Element => (
  <section className="rounded bg-slate-900 p-4 text-sm text-slate-200">Rollback executor connected to snapshot metadata and integrity checks.</section>
);
